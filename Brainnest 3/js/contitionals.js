     // displays the greater of the two numbers.
let a=100;
let b=15;
if(a>b){
   document.write('A is greater ')
  }
 else{ 
      document.write('B is greater') 
    }


     //odd or even numbers   
     const number = prompt("Enter a number: ");
     const result = (number % 2  == 0) ? "even" : "odd";
     console.log(`The number is ${result}.`);
